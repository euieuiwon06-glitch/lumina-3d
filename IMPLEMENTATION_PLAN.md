# LUMINA 3D 데모 — 구현 계획과 진행 기록

2026-09-14 · 기준 문서: `../LUMINA_게임기획안_v2.md`, `../01_확정_디자인가이드.md`, `../02_이미지유지_구현명세.md`, `../03_CLAUDE_CODE_작업지시.md`

## 1. 방향 전환: 고정 카메라 2.5D → 블렌더 모델 기반 실제 3D

기존 `../lumina`(2.5D)는 승인 이미지를 배경으로 쓰는 고정 카메라 데모였다. 이후 모든 배경·캐릭터가 블렌더로 제작되었고(`../../비주얼/LUMINA_추가배경_제작목록/Blender`, `../../비주얼/블렌더/캐릭터모델링.blend`), 추가배경 README도 "정지 이미지를 평면에 붙이는 방식으로 되돌리지 않고 실제 3D 공간에서 카메라 회전·이동을 지원"하는 방향을 명시했다. 그래서 새 데모는 **블렌더 파일을 직접 변환해 360° 회전 가능한 3D 공간**으로 만든다. 2.5D 데모는 수정하지 않고 그대로 둔다.

| 항목 | 2.5D(`../lumina`) | 3D(이 프로젝트) |
|---|---|---|
| 배경 | 승인 이미지 1장 | 블렌더 장면 8종 → GLB(근경) + Cycles 360° 파노라마(원경·하늘) |
| 캐릭터 | 모델 렌더 스프라이트 4방향 | 캐릭터모델링.blend 6종 GLB, 파츠 회전으로 걷기·인사 |
| 카메라 | 고정 | 3인칭 궤도 360°(드래그·Q/R·버튼), 휠 확대, 벽 충돌 |
| 이동 영역 | 손으로 그린 다각형 | 블렌더 `ENV_*_Walkable` 컬렉션에서 구운 보행 격자(여러 층) + A* |
| 게임 상태·UI | — | 2.5D의 퀘스트 판정·보상 원장·저장 검증·퍼즐·HUD·모달을 이어받아 확장 |

## 2. 구조

```text
tools/blender/export_scene.py       .blend → GLB·파노라마·동선/조명/보행 격자 JSON (원본 저장 안 함)
tools/blender/export_characters.py  캐릭터 6종 → GLB(발바닥 원점, 팔·다리 회전축)
tools/blender/common.py             절차적 재질 → glTF 값 환산
tools/export-all.mjs                일괄 변환
tools/playtest.mjs                  전체 흐름 자동 플레이(Playwright)
tools/gridcheck.mjs, gridview.mjs   보행 격자 연결 검사·평면도
src/engine/  world(로더·조명 풀) camera(궤도) character(루미 애니) walkgrid(격자·A*) props(빛·결정·표시) stage(UI 무대)
src/game/    state(저장·전이) quests(판정) catalog puzzle scenes(장면 배치)
src/ui/      hud panels(설치·작업대·퍼즐·안내) modal overlays(인트로·항해·마무리·전환)
tests/       logic.test.js, walkgrid.test.js
```

## 3. 장면 연결과 데모 흐름

```text
빛 제작실(작업대) ─ 캡슐 마을(포근·쉼터) ─ 촉수 산책로(살구·봉오리·숨은 씨앗) ─ 항해 전망대(보라·빛 오르간)
      └ 씨앗 온실(씨앗 연못)                                                         │ 출항(빛의 온도)
                                                   얼음 성운(리본·노래하는 결정) ◄──┤ 차가움
                                                   태양 정원(태양씨앗 숲)       ◄──┤ 따뜻함
                                                   황혼 합류지(공명 조각)       ◄──┘ 중간
```

1. 조용한 쉼터(포근): 작업대에서 맥동·밝기 60 이하 → 쉼터에 놓기. 틀리면 거두고 다시.
2. 살구빛 산책로(살구): 산책로 가운데 봉오리에 따뜻한 빛 → 노을빛 배색.
3. 첫 항로(보라): 빛 오르간에 차가운 빛 → 출항 → 얼음 성운 도착.
4. 결정의 노래(리본): 3D 결정 3개가 빛+소리로 4음 → 따라 누르기(실패 위치 표시·다시 듣기·다시 하기) → 천천히 맥동하는 기억.
5. 산책로의 마지막 빛(포근): 천천히 맥동 빛을 산책로 끝에 → 잠든 봉오리가 함께 깨어남 → 마무리.

## 4. 체크리스트

- [x] 블렌더 파일 11개 조사(컬렉션·재질 노드·폴리곤·동선 표시·카메라)
- [x] 장면 변환 파이프라인(재질 환산, 커브→메시, 무거운 오브젝트 감면, Draco)
- [x] 360° 파노라마(Cycles OPTIX, 원경 컬렉션 + 월드)
- [x] 보행 격자: 위를 향한 면·머리 공간·장애물 반경·경사 한도·최대 연결 영역
- [x] 캐릭터 6종 GLB, 팔·다리 회전축, 손·발 계층
- [x] 3인칭 360° 카메라, 벽 충돌(BVH), 가까운 주민 숨김
- [x] WASD·클릭 이동(A* 경로), 경유점 막힘 복구
- [x] 상호작용: 주민·설치 지점·작업대·출구(잠김 사유)·발견물·퍼즐
- [x] 퀘스트 5종 조건 판정, 보상 원장, 저장 버전·검증, 장면별 위치 저장
- [x] 항해 목적지 3곳, 귀환
- [x] 자동 테스트 14개, 전체 흐름 자동 플레이 37개 확인
- [x] 톤: 배경 AgX + 채도 보정(원본 AgX Punchy), 캐릭터는 원본 Standard 뷰 유지
- [ ] 실제 GPU(RTX) 브라우저에서 사람이 직접 플레이하며 조작감 조정 — 헤드리스 검수만 함
- [ ] 교환·공명·멀티플레이(기획 후속 범위)
